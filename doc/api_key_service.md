# class KeyService

## create(conf)

**input**

* `conf` / Object / {}
* `conf.role` / string, one of `r`, `w`, `rw`

**ouput** - Object

* `type` / string / 'key'
* `expires_at` / integer / 0
* `access_token` / string
